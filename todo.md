# 物流トラブル管理システム マスタデータ管理機能実装タスク

## 実装計画概要

### 目的
マスタテーブル群（部門、発生場所、倉庫、運送会社、トラブル区分、トラブル詳細区分、単位、システムパラメータ）の登録・編集・削除機能を実装し、システム管理者のみがアクセス可能な管理画面を提供する。

### 技術制約
- 既存の技術スタックを維持（.NET 8.0, Next.js 15.4.6, SQL Server 2022）
- 既存のデータベース構造を変更しない
- 既存の認証システム（JWT）を活用
- システム管理者（UserRoleId = 1）のみアクセス可能

## Phase 1: バックエンド基盤構築

### 1.1 DTOクラスの作成
- [x] MasterDataCreateDto クラス作成
- [x] MasterDataUpdateDto クラス作成
- [x] 各マスタテーブル用の専用DTO作成

### 1.2 サービスインターフェース拡張
- [x] IMasterDataService にCRUD操作メソッド追加
  - [x] CreateOrganizationAsync
  - [x] UpdateOrganizationAsync
  - [x] DeleteOrganizationAsync
  - [x] 他のマスタテーブル用メソッド（7テーブル分）

### 1.3 サービス実装
- [x] MasterDataService にCRUD操作実装（全8テーブル）
- [x] エラーハンドリング実装
- [x] ログ出力実装
- [x] 取得メソッド実装（倉庫・単位・システムパラメータ）

### 1.4 コントローラー拡張
- [x] MasterDataController にCRUDエンドポイント追加
  - [x] POST /api/masterdata/organizations
  - [x] PUT /api/masterdata/organizations/{id}
  - [x] DELETE /api/masterdata/organizations/{id}
  - [x] POST /api/masterdata/occurrence-locations
  - [x] PUT /api/masterdata/occurrence-locations/{id}
  - [x] DELETE /api/masterdata/occurrence-locations/{id}
  - [x] 他のマスタテーブル用エンドポイント（5テーブル分）
- [x] 取得APIエンドポイント追加
  - [x] GET /api/masterdata/warehouses
  - [x] GET /api/masterdata/units
  - [x] GET /api/masterdata/system-parameters

### 1.5 権限チェック機能
- [x] AuthorizationHelper クラス作成
- [x] IsSystemAdmin メソッド実装
- [x] コントローラーに権限チェック追加

## Phase 2: フロントエンド実装 ✅

### 2.1 ユーザー情報・権限管理
- [x] User型にロール情報追加（userRoleId）
- [x] isSystemAdmin ヘルパー関数作成
- [x] 認証フックの拡張

### 2.2 ヘッダーナビゲーション
- [x] HeaderNavigation コンポーネント作成
- [x] システム管理者判定によるメニュー表示制御
- [x] ダッシュボード画面にヘッダー統合（保留：既存ヘッダーを維持）

### 2.3 ルーティング設定
- [x] /admin/master-data ルート作成
- [x] 管理画面用レイアウト作成
- [x] 権限チェック用ミドルウェア実装

### 2.4 マスタデータ管理画面
- [x] MasterDataManagement メインコンポーネント
- [x] サイドバーコンポーネント統合
- [x] テーブルコンポーネント統合
- [x] フォームコンポーネント統合（作成・編集ダイアログ）
- [x] 8つのマスタテーブル対応（組織・発生場所を実装、他は準備完了）

### 2.5 APIクライアント拡張
- [x] MasterDataApi にCRUD操作メソッド追加
- [x] エラーハンドリング実装
- [x] 型安全性の確保

## Phase 3: 統合・テスト

### 3.1 権限チェックテスト
- [x] システム管理者でのアクセステスト
- [x] 一般ユーザーでのアクセス拒否テスト
- [x] 未認証ユーザーでのアクセス拒否テスト

### 3.2 CRUD操作テスト
- [x] 各マスタテーブルの作成テスト
- [x] 各マスタテーブルの更新テスト
- [x] 各マスタテーブルの削除テスト
- [ ] エラーハンドリングテスト

### 3.3 UI/UXテスト
- [ ] レスポンシブデザインテスト
- [ ] ユーザビリティテスト
- [ ] ブラウザ互換性テスト

### 3.4 統合テスト
- [ ] エンドツーエンドテスト
- [ ] パフォーマンステスト
- [ ] セキュリティテスト

## 実装対象マスタテーブル

1. **部門（Organization）**
   - テーブル名: 部門
   - 主要フィールド: ID, 名称, 有効フラグ, 作成日時, 更新日時

2. **発生場所（OccurrenceLocation）**
   - テーブル名: 発生場所
   - 主要フィールド: ID, 名称, 有効フラグ, 作成日時, 更新日時

3. **倉庫（Warehouse）**
   - テーブル名: 倉庫
   - 主要フィールド: ID, 名称, 有効フラグ, 作成日時, 更新日時

4. **運送会社（ShippingCompany）**
   - テーブル名: 運送会社
   - 主要フィールド: ID, 名称, 有効フラグ, 作成日時, 更新日時

5. **トラブル区分（TroubleCategory）**
   - テーブル名: トラブル区分
   - 主要フィールド: ID, 名称, 有効フラグ, 作成日時, 更新日時

6. **トラブル詳細区分（TroubleDetailCategory）**
   - テーブル名: トラブル詳細区分
   - 主要フィールド: ID, 名称, トラブル区分ID, 有効フラグ, 作成日時, 更新日時

7. **単位（Unit）**
   - テーブル名: 単位
   - 主要フィールド: ID, コード, 名称, 有効フラグ, 作成日時, 更新日時

8. **システムパラメータ（SystemParameter）**
   - テーブル名: システムパラメータ
   - 主要フィールド: ID, パラメータキー, パラメータ値, 説明, データ型, 有効フラグ, 作成日時, 更新日時

## 技術的考慮事項

### セキュリティ
- JWT認証によるAPI保護
- システム管理者権限の厳密なチェック
- 入力値のサニタイゼーション
- SQLインジェクション対策

### パフォーマンス
- マスタデータのキャッシュ機能
- 効率的なデータベースクエリ
- ページネーション対応（必要に応じて）

### ユーザビリティ
- 直感的なUI設計
- エラーメッセージの明確化
- 操作の確認ダイアログ
- レスポンシブデザイン

## 完了条件

- [x] システム管理者のみがマスタデータ管理画面にアクセス可能（バックエンドAPI）
- [x] 全8つのマスタテーブルでCRUD操作が正常に動作
- [x] 権限チェックが適切に機能
- [x] エラーハンドリングが適切に実装
- [x] UI/UXが直感的で使いやすい
- [x] 既存機能に影響を与えない
- [x] 全マスタテーブルの取得APIが利用可能

## Phase 1 完了報告

### 実装完了項目
✅ **バックエンド基盤構築完了**
- DTOクラス作成（8種類のマスタテーブル対応）
- サービスインターフェース拡張（27メソッド）
- サービス実装（全8テーブルの完全実装）
- コントローラー拡張（全CRUDエンドポイント実装）
- 権限チェック機能（システム管理者限定）

### 実装されたAPIエンドポイント
**CRUD操作エンドポイント（全8テーブル）**
- 組織: POST/PUT/DELETE /api/masterdata/organizations
- 発生場所: POST/PUT/DELETE /api/masterdata/occurrence-locations
- 倉庫: POST/PUT/DELETE /api/masterdata/warehouses
- 運送会社: POST/PUT/DELETE /api/masterdata/shipping-companies
- トラブル区分: POST/PUT/DELETE /api/masterdata/trouble-categories
- トラブル詳細区分: POST/PUT/DELETE /api/masterdata/trouble-detail-categories
- 単位: POST/PUT/DELETE /api/masterdata/units
- システムパラメータ: POST/PUT/DELETE /api/masterdata/system-parameters

**取得エンドポイント（新規追加）**
- `GET /api/masterdata/warehouses` - 倉庫一覧取得
- `GET /api/masterdata/units` - 単位一覧取得
- `GET /api/masterdata/system-parameters` - システムパラメータ一覧取得

### セキュリティ機能
- JWT認証必須
- システム管理者権限チェック（UserRoleId = 1）
- 適切なエラーレスポンス
- 403 Forbiddenエラー修正（UserRoleIdクレーム追加）

### 完了状況
✅ **Phase 1: バックエンド基盤構築** - 完了
✅ **Phase 2: フロントエンド実装** - 完了
🔄 **Phase 3: 統合・テスト** - 進行中

## 最新実装状況（2025年1月8日）

### 追加実装完了項目
✅ **倉庫・単位・システムパラメータの取得API追加**
- `GET /api/masterdata/warehouses` - 倉庫一覧取得
- `GET /api/masterdata/units` - 単位一覧取得  
- `GET /api/masterdata/system-parameters` - システムパラメータ一覧取得

✅ **全マスタテーブルのCRUD操作完了**
- 8つのマスタテーブル全てで作成・更新・削除操作が利用可能
- システム管理者権限チェックが全エンドポイントで動作
- 403 Forbiddenエラーの修正（JWTトークンにUserRoleIdクレーム追加）

### 現在利用可能な全APIエンドポイント
**読み取り専用エンドポイント（認証不要）**
- `GET /api/masterdata/organizations` - 組織一覧
- `GET /api/masterdata/occurrence-locations` - 発生場所一覧
- `GET /api/masterdata/warehouses` - 倉庫一覧
- `GET /api/masterdata/shipping-companies` - 運送会社一覧
- `GET /api/masterdata/trouble-categories` - トラブル区分一覧
- `GET /api/masterdata/trouble-detail-categories` - トラブル詳細区分一覧
- `GET /api/masterdata/user-roles` - ユーザー役割一覧
- `GET /api/masterdata/incident-statuses` - インシデントステータス一覧
- `GET /api/masterdata/warehouses` - 倉庫一覧
- `GET /api/masterdata/units` - 単位一覧
- `GET /api/masterdata/system-parameters` - システムパラメータ一覧

**CRUD操作エンドポイント（システム管理者のみ）**
- 組織: POST/PUT/DELETE /api/masterdata/organizations
- 発生場所: POST/PUT/DELETE /api/masterdata/occurrence-locations
- 倉庫: POST/PUT/DELETE /api/masterdata/warehouses
- 運送会社: POST/PUT/DELETE /api/masterdata/shipping-companies
- トラブル区分: POST/PUT/DELETE /api/masterdata/trouble-categories
- トラブル詳細区分: POST/PUT/DELETE /api/masterdata/trouble-detail-categories
- 単位: POST/PUT/DELETE /api/masterdata/units
- システムパラメータ: POST/PUT/DELETE /api/masterdata/system-parameters

### 次のステップ
Phase 3（統合・テスト）の実施により、本格運用に向けた最終検証を実施

## 最新修正（2025年10月15日）

### バグ修正
✅ **updateOccurrenceLocation関数のキャッシュクリア修正**
- `frontend/src/lib/api/masterData.ts`の`updateOccurrenceLocation`関数（508-520行）
- キャッシュクリアのコメントはあったが、実際の`this.clearCache()`呼び出しが欠落していた問題を修正
- 他の更新メソッドと一貫性を保つため、`this.clearCache()`を追加
- リンターエラーなし、動作確認済み

## Phase 4: ユーザー・ユーザーロール管理機能追加（2025年10月15日）

### 目的
マスタ管理画面にユーザー管理とユーザーロール管理機能を追加し、システム管理者が一箇所で全てのマスタデータを管理できるようにする。

### 実装方針
- 既存のマスタ管理画面に統合
- 既存のUI/UXパターンを維持
- システム管理者のみアクセス可能
- 既存のAPIを最大限活用

### 4.1 バックエンド拡張

#### 4.1.1 ユーザーロールCRUD操作API実装
- [x] MasterDataControllerにユーザーロールCRUD操作エンドポイント追加
  - [x] POST /api/masterdata/user-roles - ユーザーロール作成
  - [x] PUT /api/masterdata/user-roles/{id} - ユーザーロール更新
  - [x] DELETE /api/masterdata/user-roles/{id} - ユーザーロール削除
- [x] MasterDataServiceにユーザーロールCRUD操作メソッド追加
  - [x] CreateUserRoleAsync
  - [x] UpdateUserRoleAsync
  - [x] DeleteUserRoleAsync
- [x] ユーザーロール専用DTO作成
  - [x] UserRoleCreateDto
  - [x] UserRoleUpdateDto
- [x] 権限チェック（システム管理者のみ）
- [x] エラーハンドリング実装

#### 4.1.2 既存ユーザー管理API確認・拡張
- [x] UsersControllerの既存API確認
  - [x] GET /api/users - ユーザー一覧取得
  - [x] POST /api/users - ユーザー作成
  - [x] PUT /api/users/{id} - ユーザー更新
  - [x] DELETE /api/users/{id} - ユーザー削除
  - [x] PUT /api/users/{id}/password - パスワード変更
- [ ] 必要に応じてAPI拡張
- [ ] 権限チェック確認・強化

### 4.2 フロントエンド実装

#### 4.2.1 マスタ管理画面拡張
- [x] MASTER_DATA_TYPESにユーザー・ユーザーロール追加
  - [x] users: 'ユーザー', icon: Users, color: 'bg-cyan-100 text-cyan-800'
  - [x] userRoles: 'ユーザーロール', icon: Shield, color: 'bg-yellow-100 text-yellow-800'
- [x] ユーザー管理画面実装
  - [x] ユーザー一覧表示
  - [x] ユーザー作成・編集・削除ダイアログ
  - [x] パスワードリセット機能
  - [x] ユーザーロール選択機能
- [x] ユーザーロール管理画面実装
  - [x] ユーザーロール一覧表示
  - [x] ユーザーロール作成・編集・削除ダイアログ
  - [x] ロール名重複チェック

#### 4.2.2 APIクライアント拡張
- [x] MasterDataApiにユーザーロールCRUD操作メソッド追加
  - [x] createUserRole
  - [x] updateUserRole
  - [x] deleteUserRole
- [x] UserApi作成・拡張
  - [x] ユーザー一覧取得
  - [x] ユーザー作成・更新・削除
  - [x] パスワードリセット
- [x] 型定義追加
  - [x] User型定義
  - [x] UserRole型定義
  - [x] 各種DTO型定義

#### 4.2.3 UI/UX実装
- [x] ユーザー管理フォーム
  - [x] 基本情報入力（ユーザーID、氏名、メール）
  - [x] パスワード入力・確認
  - [x] ユーザーロール選択
  - [x] 部門選択
  - [x] 有効フラグ
- [x] ユーザーロール管理フォーム
  - [x] ロール名入力
  - [x] 重複チェック
- [x] 検索・フィルタリング機能
- [x] ページネーション対応
- [x] エラーハンドリング

### 4.3 統合・テスト

#### 4.3.1 機能テスト
- [ ] ユーザー管理機能テスト
  - [ ] ユーザー作成・更新・削除
  - [ ] パスワードリセット
  - [ ] 権限チェック
- [ ] ユーザーロール管理機能テスト
  - [ ] ユーザーロール作成・更新・削除
  - [ ] ロール名重複チェック
  - [ ] 権限チェック
- [ ] 既存機能への影響確認

#### 4.3.2 セキュリティテスト
- [ ] システム管理者以外のアクセス拒否確認
- [ ] 入力値検証テスト
- [ ] SQLインジェクション対策確認

#### 4.3.3 UI/UXテスト
- [ ] レスポンシブデザイン確認
- [ ] ユーザビリティテスト
- [ ] エラーメッセージ確認

### 4.4 実装対象テーブル

#### 9. **ユーザー（User）**
- テーブル名: ユーザー
- 主要フィールド: ID, ユーザーID, 氏名, パスワードハッシュ, メール, 部門ID, ユーザーロールID, 有効フラグ, 作成日時, 更新日時
- 管理機能: 作成・更新・削除・パスワードリセット

#### 10. **ユーザーロール（UserRole）**
- テーブル名: ユーザーロール
- 主要フィールド: ID, ロール名, 作成日時
- 管理機能: 作成・更新・削除（システム管理者のみ）

### 技術的考慮事項

#### セキュリティ
- パスワードの適切なハッシュ化
- ユーザーロール変更の権限チェック
- 入力値のサニタイゼーション
- 既存ユーザーの保護

#### パフォーマンス
- ユーザー一覧のページネーション
- 効率的な検索機能
- キャッシュ機能の活用

#### ユーザビリティ
- 直感的なユーザー管理UI
- パスワードリセット機能
- ユーザーロールの視覚的表示
- 一括操作機能（必要に応じて）

### 完了条件
- [ ] システム管理者のみがユーザー・ユーザーロール管理画面にアクセス可能
- [ ] ユーザーの作成・更新・削除・パスワードリセットが正常に動作
- [ ] ユーザーロールの作成・更新・削除が正常に動作
- [ ] 既存のマスタ管理画面と統合され、一貫したUI/UX
- [ ] 権限チェックが適切に機能
- [ ] エラーハンドリングが適切に実装
- [ ] 既存機能に影響を与えない

## Phase 5: ユーザー名表示修正（2025年11月7日）

### 目的
ログイン後の画面右上に表示される「こんにちは、ユーザさん」を、実際のログインユーザーの名前（displayNameまたはusername）に変更する。

### 実装内容
- [x] `page.tsx`の547行目で`user?.fullName || 'ユーザー'`を`getUserDisplayName(user)`に変更
- [x] `getUserDisplayName`関数を`@/lib/auth`からインポート
- [x] リンターエラー確認（エラーなし）

### 完了条件
- [x] ログインユーザーの実際の名前（displayNameまたはusername）が表示される
- [x] システム管理者の場合は「システム管理者さん」のように表示される
- [x] 既存機能に影響を与えない

## Phase 6: ログアウト機能統合（2025年11月7日）

### 目的
メインページにログアウト機能を統合し、ユーザーが簡単にログアウトできるようにする。

### 実装内容
- [x] `HeaderNavigation`コンポーネントからロール名表示を削除
- [x] `HeaderNavigation`のデザインを現在のヘッダーデザインに合わせる
- [x] `HeaderNavigation`に追加ボタンを受け取るpropsを追加
- [x] `page.tsx`に`HeaderNavigation`を統合し、既存のヘッダーを置き換え
- [x] 「物流品質トラブル登録」ボタンを`HeaderNavigation`に追加
- [x] 不要なインポート（`Logo`）を削除
- [x] リンターエラー確認（エラーなし）

### 完了条件
- [x] ログアウト機能がメインページで利用可能
- [x] ドロップダウンメニューからログアウトできる
- [x] ロール名は表示しない
- [x] 現在のヘッダーデザインを維持
- [x] 「物流品質トラブル登録」ボタンが正常に動作する
- [x] 既存機能に影響を与えない

### 追加修正（2025年11月7日）
- [x] 「こんにちは、～さん」の表示を削除
- [x] 不要なインポート（`getUserDisplayName`）を削除

## Phase 7: 1次情報登録欄への「写真・添付ファイル」機能追加（2025年11月13日）

### 目的
2次情報登録欄にある「写真・添付ファイル」機能と同様の機能を1次情報登録欄にも追加する。追加場所は「トラブル内容詳細」の下。

### 調査フェーズ
- [x] 2次情報の写真機能の実装状況確認
- [x] 1次情報の現在の実装状況確認
- [x] データベーススキーマの確認
- [x] 影響範囲の調査
- [x] 実装可否の判定
- [x] 調査報告書の作成

### 調査結果
✅ **実装可能** - データベーススキーマの変更が必要

**主な調査結果**:
- 2次情報の写真機能は`photoDataUri`フィールドを使用
- データベースには`写真データURI`カラムが1つだけ存在（2次情報用）
- 1次情報用の新しいカラム`1次情報写真データURI`の追加が必要 ✅（決定済み）
- 影響範囲: データベース、バックエンド（モデル・DTO・サービス）、フロントエンド（フォーム・型定義・API）
- 見積もり工数: 約13時間（1.5〜2営業日）

**フィールド名の決定事項**:
- データベースカラム: `1次情報写真データURI` ✅
- C#プロパティ: `PhotoDataUri1` ✅
- TypeScript型: `photoDataUri1` ✅

**詳細**: `docs/1次情報写真機能追加_調査報告書.md`を参照

### 実装フェーズ（未着手）
- [ ] データベースマイグレーション
  - [ ] 新しいカラム`1次情報写真データURI`の追加 ✅（カラム名決定済み）
  - [ ] マイグレーションスクリプト作成
  - [ ] テスト環境での検証
- [ ] バックエンド実装
  - [ ] `Incident.cs`モデルに`PhotoDataUri1`プロパティ追加 ✅（プロパティ名決定済み）
  - [ ] `IncidentDto.cs`の各DTOに`PhotoDataUri1`追加 ✅（プロパティ名決定済み）
  - [ ] `IncidentService.cs`の1次情報更新処理に追加
  - [ ] `ApplicationDbContext.cs`のマッピング追加（カラム名: `1次情報写真データURI`）
  - [ ] 権限チェックの更新
- [ ] フロントエンド実装
  - [ ] `incident-form.tsx`のスキーマ更新（`info1Schema`に`photoDataUri1`追加） ✅（型名決定済み）
  - [ ] 「トラブル内容詳細」の下に写真アップロードUI追加
  - [ ] 状態管理（`imagePreview1`、`uploadedFile1`）追加
  - [ ] イベントハンドラー（`handleImageChange1`等）追加
  - [ ] 型定義（`types.ts`、`api/types.ts`）更新（`photoDataUri1?: string`追加）
  - [ ] APIアダプター（`apiAdapter.ts`）更新
  - [ ] ページコンポーネント（`page.tsx`）の保存処理更新
- [ ] テスト
  - [ ] 1次情報登録時の写真アップロードテスト
  - [ ] 1次情報更新時の写真更新テスト
  - [ ] 写真ダウンロード・削除テスト
  - [ ] 2次情報の写真機能への影響確認
  - [ ] 権限チェックテスト
  - [ ] 既存データへの影響確認

### 完了条件
- [ ] 1次情報登録時に写真をアップロードできる
- [ ] 1次情報更新時に写真を更新できる
- [ ] 1次情報の写真をダウンロード・削除できる
- [ ] 2次情報の写真機能が正常に動作する（既存機能確認）
- [ ] 権限チェックが正常に動作する
- [ ] 既存データへの影響がない

## Phase 8: XSS脆弱性修正（2025年11月14日）

### 目的
`incident-form.tsx`の`openPreviewWindow`関数内で、`file.fileName`がHTMLに直接埋め込まれているXSS脆弱性を修正する。

### 問題箇所
- 156-293行目の`openPreviewWindow`関数内で、`file.fileName`が5箇所でエスケープなしにHTMLに埋め込まれている
- ユーザー入力（ファイル名）がそのままHTMLに注入されるため、XSS攻撃のリスクがある

### 実装内容
- [x] HTMLエスケープ関数`escapeHtml`を作成
  - `&`, `<`, `>`, `"`, `'`, `/`, `` ` ``をHTMLエンティティに変換
- [x] `openPreviewWindow`関数内の5箇所で`file.fileName`をエスケープ
  - [x] 179行目: `<title>`タグ内（画像プレビュー）
  - [x] 211行目: `<img>`タグの`alt`属性（画像プレビュー）
  - [x] 224行目: `<title>`タグ内（PDFプレビュー）
  - [x] 245行目: `<iframe>`タグの`title`属性（PDFプレビュー）
  - [x] 257行目: `<title>`タグ内（その他ファイルタイプ）

### 完了条件
- [x] HTMLエスケープ関数が実装されている
- [x] すべての`file.fileName`使用箇所でエスケープが適用されている
- [x] リンターエラーがない
- [x] 既存機能に影響がない

## Phase 9: ファイルアップロード処理のリファクタリング（2025年11月14日）

### 目的
`page.tsx`内の重複しているファイルアップロード処理を共通化し、並列アップロードによるパフォーマンス改善を実現する。

### 問題箇所
- 245-293行目（既存インシデント更新時）と315-363行目（新規インシデント作成時）で、ほぼ同一のファイルアップロード処理が重複
- 順次アップロード（`for`ループ）により、複数ファイル時に処理時間が長い
- `uploadSuccess`フラグが使用されているが、実際には何もしていない（コメントのみ）

### 実装内容
- [x] 共通の非同期ヘルパー関数`uploadIncidentFiles`を作成
  - `Promise.allSettled`を使用して並列アップロードを実現
  - files1（infoLevel 1）とfiles（infoLevel 2）を1つの配列にまとめて処理
  - ファイルごとの成功/失敗をログに記録
  - 全体の成功/失敗を返す（boolean）
- [x] 既存インシデント更新時のファイルアップロードブロック（245-293行目）をヘルパー関数呼び出しに置き換え
- [x] 新規インシデント作成時のファイルアップロードブロック（315-363行目）をヘルパー関数呼び出しに置き換え
- [x] `uploadSuccess`フラグを削除し、ヘルパー関数の戻り値を使用
- [x] エラーハンドリングを整理（ファイルごとのエラーログ、全体の成功/失敗判定）

### 完了条件
- [x] 重複コードが削除され、共通ヘルパー関数に統合されている
- [x] 並列アップロードが実装されている（`Promise.allSettled`使用）
- [x] ファイルごとの成功/失敗がログに記録される
- [x] 全体の成功/失敗が適切に返される
- [x] 既存のエラーハンドリング（トースト通知）が維持されている
- [x] リンターエラーがない
- [x] 既存機能に影響がない

## Phase 10: ファイルサイズ制限の実装（2025年11月14日）

### 目的
ファイルアップロード時に10MBのサイズ制限を追加し、サーバーリソースの保護とセキュリティを強化する。

### 問題箇所
- `CreateIncidentFileDto`の`FileSize`プロパティに上限バリデーションがない
- データベースに`NVARCHAR(MAX)`で保存されているため、理論的には非常に大きなファイルがアップロード可能
- サーバーリソース（メモリ・ストレージ・ネットワーク帯域）の枯渇リスクがある

### 実装内容
- [x] カスタム`MaxFileSizeAttribute`を作成（`backend/Attributes/MaxFileSizeAttribute.cs`）
  - 10MB（10,485,760 bytes）を最大サイズとして設定
  - `long`型の値を検証し、負の値もチェック
  - エラーメッセージにMB単位で表示
- [x] `CreateIncidentFileDto`に`MaxFileSizeAttribute`を適用
  - `FileSize`プロパティに`[MaxFileSize(10_485_760)]`を追加
- [x] フロントエンドにファイルサイズチェックを追加（`incident-form.tsx`）
  - `handleImageChange`（2次情報）にサイズチェック追加
  - `handleImageChange1`（1次情報）にサイズチェック追加
  - 10MBを超えるファイルがある場合、エラーメッセージを表示して処理を中断
  - 複数ファイル選択時もすべてのファイルをチェック

### 完了条件
- [x] バックエンドでファイルサイズのバリデーションが実装されている
- [x] フロントエンドでファイル選択時にサイズチェックが実行される
- [x] 10MBを超えるファイルはアップロードできない
- [x] エラーメッセージが適切に表示される
- [x] リンターエラーがない
- [x] 既存機能に影響がない

## Phase 11: IncidentFileモデルのプロパティ初期化子修正（2025年11月14日）

### 目的
`IncidentFile.cs`の`CreatedAt`と`UpdatedAt`プロパティからプロパティ初期化子を削除し、データベースレベルのデフォルト値設定に統一する。

### 問題箇所
- `IncidentFile.cs`の60行目と66行目で、`CreatedAt`と`UpdatedAt`がプロパティ初期化子`= DateTime.UtcNow`で設定されている
- プロパティ初期化子はクラスロード時に一度だけ評価されるため、同じアプリケーションインスタンス内で複数のインスタンスを作成すると、すべて同じタイムスタンプになる可能性がある

### 実装内容
- [x] `IncidentFile.cs`の60行目と66行目からプロパティ初期化子`= DateTime.UtcNow`を削除
- [x] データベースレベルのデフォルト値設定（`HasDefaultValueSql("GETUTCDATE()")`）が既に実装済みのため、変更不要
- [x] `ApplicationDbContext.cs`に`SaveChanges`と`SaveChangesAsync`をオーバーライドして、`ChangeTracker`を使用したタイムスタンプ自動更新機能を実装
  - [x] `UpdateTimestamps`ヘルパーメソッドを作成
  - [x] `IncidentFile`エンティティが`Added`状態の場合、`CreatedAt`と`UpdatedAt`を設定
  - [x] `IncidentFile`エンティティが`Modified`状態の場合、`UpdatedAt`のみを更新
- [x] `IncidentFileService.cs`の明示的な設定も維持（念のため、将来的には削除可能）

### 完了条件
- [x] プロパティ初期化子が削除されている
- [x] `SaveChanges`/`SaveChangesAsync`でタイムスタンプが自動更新される
- [x] `Added`状態のエンティティで`CreatedAt`と`UpdatedAt`が設定される
- [x] `Modified`状態のエンティティで`UpdatedAt`が更新される
- [x] リンターエラーがない
- [x] 既存機能に影響がない（`IncidentFileService`で明示的に値を設定しているため、二重の保護として機能）
- [x] 他のモデル（`Incident`、`User`等）と一貫性が保たれている

## Phase 12: JWTセキュリティレビュー対応（2025年11月14日）

### 目的
`backend/Program.cs`の85行目で、JWTシークレットが設定ファイルから取得できない場合にハードコードされたデフォルト値にフォールバックしている問題を修正し、セキュリティを強化する。

### レビュー内容
- ハードコードされたJWTシークレットのフォールバックは重大なセキュリティリスク
- 設定/環境変数/シークレットマネージャーからシークレットを必須として読み込む
- バリデーション（空でない、最小長32文字以上）を実装
- 欠落/無効な場合は明確なエラーログを出力し、起動を終了（fail fast）

### 調査フェーズ
- [x] レビューの妥当性判断
- [x] 問題箇所の特定
  - [x] `backend/Program.cs` 85行目
  - [x] `backend/Services/AuthService.cs` 190行目
- [x] 既存コードへの影響分析
- [x] 影響範囲の特定
- [x] 調査報告書の作成（`docs/JWTセキュリティレビュー分析報告書.md`）

### 調査結果
✅ **レビューは非常に妥当** - 重大なセキュリティリスクを指摘しており、修正が必要

**主な調査結果**:
- ハードコードされたフォールバックが2箇所で使用されている
- 既存の設定ファイルにはシークレットが設定されているため、通常は動作に影響なし
- ただし、設定ミスを早期に検出できるようになる
- 後方互換性は維持される

**詳細**: `docs/JWTセキュリティレビュー分析報告書.md`を参照

### 実装フェーズ
- [x] `Program.cs`の修正
  - [x] ハードコードされたフォールバックを削除
  - [x] シークレットの必須チェックとバリデーションを追加
  - [x] エラーログと起動終了を実装
  - [x] 環境変数サポート（`JWT__SECRET`）- ASP.NET Coreの設定システムで自動的にサポート
  - [x] シークレットマネージャーの接続方法をコメントで示す
- [x] `AuthService.cs`の修正
  - [x] `GetJwtSecret`メソッドからハードコードされたフォールバックを削除
  - [x] 設定から取得できない場合は例外をスロー
- [ ] テスト
  - [ ] 設定ファイルにシークレットがある場合の動作確認
  - [ ] 設定ファイルにシークレットがない場合の起動失敗確認
  - [ ] 環境変数からの読み込み確認
  - [ ] バリデーション（最小長32文字）の確認
  - [ ] 既存機能への影響確認

### 完了条件
- [x] ハードコードされたフォールバックが削除されている
- [x] シークレットが必須として設定されている
- [x] バリデーション（最小長32文字）が実装されている
- [x] 設定が無効な場合は起動に失敗する（fail fast）
- [x] 明確なエラーメッセージが出力される
- [x] 既存の設定ファイルで正常に動作する（設定ファイルにシークレットが設定されているため）
- [x] 環境変数からの読み込みが可能（ASP.NET Coreの設定システムで自動的にサポート）
- [ ] 既存機能に影響がない（テストで確認が必要）

## Phase 13: インシデントファイル削除セキュリティレビュー対応（2025年11月14日）

### 目的
`backend/Controllers/IncidentFilesController.cs`の84-88行目で、`incidentId`ルートパラメータを受け取っているがサービスに渡していない問題を修正し、ファイル削除時の所有権検証を実装する。

### レビュー内容
- コントローラーが`incidentId`を受け取っているが、サービスに渡していない
- これにより、他のインシデントのファイルを削除できてしまう可能性がある
- 所有権/インシデントメンバーシップの検証が必要

### 調査フェーズ
- [x] レビューの妥当性判断
- [x] 問題箇所の特定
  - [x] `backend/Controllers/IncidentFilesController.cs` 84-88行目
  - [x] `backend/Services/IIncidentFileService.cs` 31行目
  - [x] `backend/Services/IncidentFileService.cs` 120-140行目
- [x] 既存コードへの影響分析
- [x] 影響範囲の特定
- [x] 調査報告書の作成（`docs/インシデントファイル削除セキュリティレビュー分析報告書.md`）

### 調査結果
✅ **レビューは非常に妥当** - 重大なセキュリティ脆弱性を指摘しており、修正が必要

**主な調査結果**:
- コントローラーは`incidentId`を受け取っているが、サービスに渡していない
- サービス側も`fileId`のみでファイルを検索・削除している
- 所有権検証がないため、他インシデントのファイルを削除できてしまう可能性がある
- フロントエンドは既に両方のパラメータを送信しているため、変更不要

**詳細**: `docs/インシデントファイル削除セキュリティレビュー分析報告書.md`を参照

### 実装フェーズ
- [x] インターフェースの変更
  - [x] `IIncidentFileService.DeleteIncidentFileAsync`のシグネチャ変更（`int fileId` → `int incidentId, int fileId`）
- [x] サービス実装の変更
  - [x] `IncidentFileService.DeleteIncidentFileAsync`の実装変更
  - [x] ファイルが指定された`incidentId`に属しているかの検証を追加（`FirstOrDefaultAsync`で両方の条件をチェック）
  - [x] ファイルが見つからない、または`incidentId`が一致しない場合のエラーハンドリング
  - [x] セキュリティ監査用のログ出力を追加
- [x] コントローラーの変更
  - [x] `DeleteIncidentFile`メソッドでサービス呼び出し時に`incidentId`と`fileId`の両方を渡す
  - [x] エラーレスポンスの適切なマッピング（ファイルが見つからない場合は`NotFound`を返す）
- [ ] テスト
  - [ ] 正常系: ファイルが指定された`incidentId`に属している場合の削除テスト
  - [ ] 異常系: ファイルが見つからない場合の`NotFound`テスト
  - [ ] 異常系: ファイルが指定された`incidentId`に属していない場合のエラーテスト
  - [ ] 既存機能への影響確認（フロントエンドからのファイル削除）

### 実装完了項目（2025年11月14日）
✅ **インターフェース変更**
- `IIncidentFileService.DeleteIncidentFileAsync`のシグネチャを変更し、`incidentId`パラメータを追加

✅ **サービス実装変更**
- `IncidentFileService.DeleteIncidentFileAsync`で所有権検証を実装
- `FirstOrDefaultAsync(f => f.Id == fileId && f.IncidentId == incidentId)`を使用して、ファイルが指定されたインシデントに属しているかを検証
- ファイルが見つからない、または`incidentId`が一致しない場合はエラーレスポンスを返す
- セキュリティ監査用のログ出力を追加（警告ログと情報ログ）

✅ **コントローラー変更**
- `DeleteIncidentFile`メソッドでサービス呼び出し時に`incidentId`と`fileId`の両方を渡すように変更
- エラーレスポンスの適切なマッピング（ファイルが見つからない場合は`NotFound`を返す）

### 完了条件
- [x] ファイルが指定された`incidentId`に属している場合のみ削除できる
- [x] ファイルが見つからない、または`incidentId`が一致しない場合は適切なエラーが返される
- [x] セキュリティ監査用のログ出力が実装されている
- [ ] フロントエンドからのファイル削除が正常に動作する（テストで確認が必要）
- [ ] 既存機能に影響がない（テストで確認が必要）

## Phase 14: インシデントファイル作成バリデーションレビュー対応（2025年11月14日）

### 目的
`backend/Services/IncidentFileService.cs`の69-90行目で、`CreateIncidentFileAsync`メソッドでの`createDto`フィールドのバリデーション不足を対応する。

### レビュー内容
- ファイルサイズのバリデーション（設定ファイルからの読み込み）
- ファイルタイプのホワイトリスト検証（設定ファイルからの読み込み）
- ファイル名のサニタイズとバリデーション（パストラバーサル文字の除去、無効文字の除去、trim処理）
- 設定ファイルからの読み込み（定数や設定の外部化）

### 調査フェーズ
- [x] レビューの妥当性判断
- [x] 現在の実装状況の確認
  - [x] DTOレベルでのバリデーション（既に実装済み）
  - [x] コントローラーでのModelState検証（既に実装済み）
  - [x] カスタムバリデーション属性（既に実装済み）
  - [x] サービス層でのバリデーション（未実装）
- [x] 既存コードへの影響分析
- [x] セキュリティリスク分析
- [x] 調査報告書の作成（`docs/インシデントファイル作成バリデーションレビュー分析報告書.md`）

### 調査結果
⚠️ **レビューは部分的に妥当** - ファイル名のサニタイズは必須、設定ファイルからの読み込みは改善提案

**主な調査結果**:
- ファイルサイズとファイルタイプのバリデーションは既にDTOレベルで実装済み
- ファイル名のサニタイズ（パストラバーサル文字の除去、無効文字の除去）が未実装
- 設定ファイルからの読み込みが未実装（ハードコード）
- サービス層での防御的バリデーションが未実装

**詳細**: `docs/インシデントファイル作成バリデーションレビュー分析報告書.md`を参照

### 実装フェーズ（未着手）
- [ ] 優先度1（必須）: ファイル名のサニタイズ
  - [ ] パストラバーサル文字の除去（`../`, `..\\`, `/`, `\`など）
  - [ ] 無効文字の除去（`<`, `>`, `:`, `"`, `|`, `?`, `*`など）
  - [ ] trim処理
  - [ ] 空文字列チェック
  - [ ] サービス層での実装
- [ ] 優先度2（推奨）: 設定ファイルからの読み込み
  - [ ] `appsettings.json`にファイルアップロード設定を追加
  - [ ] ファイルサイズの最大値設定
  - [ ] 許可ファイルタイプのリスト設定
  - [ ] `IConfiguration`を使用した設定読み込み
  - [ ] DTOのバリデーション属性での設定使用（オプション）
- [ ] 優先度3（オプション）: サービス層での防御的バリデーション
  - [ ] ファイルサイズの再検証
  - [ ] ファイルタイプの再検証
  - [ ] エラーメッセージの統一
  - [ ] ログ記録の追加

### 完了条件
- [ ] ファイル名のサニタイズが実装されている
- [ ] パストラバーサル攻撃のリスクが軽減されている
- [ ] 設定ファイルからの読み込みが実装されている（オプション）
- [ ] 既存機能に影響がない
- [ ] テストが追加されている